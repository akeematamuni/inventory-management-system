{
    "name": "metrics",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/metrics/src",
    "projectType": "library",
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "command": "tsc -p libs/metrics/tsconfig.lib.json && tsc-alias -p libs/metrics/tsconfig.lib.json"
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint"
        }
    }
}
